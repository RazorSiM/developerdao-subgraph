{
  "name": "developerdao",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ developerdao",
    "create-local": "graph create --node http://localhost:8020/ developerdao",
    "remove-local": "graph remove --node http://localhost:8020/ developerdao",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developerdao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
